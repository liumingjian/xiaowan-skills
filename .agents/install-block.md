# Install

Use the Skills CLI to install the catalog into Codex, Claude Code, or another compatible agent:

```bash
npx skills@latest add liumingjian/xiaowan-skills
```

Install a single skill with `--skill <name>`, update installed copies with `npx skills@latest update`, and use `-g` for a global install.
