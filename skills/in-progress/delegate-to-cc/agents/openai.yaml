interface:
  display_name: "Delegate to CC"
  short_description: "Get a second opinion from Claude Code on vps-2g"
  default_prompt: "Use $delegate-to-cc to get a second opinion on this project problem, then review and verify the result locally."

policy:
  allow_implicit_invocation: false
